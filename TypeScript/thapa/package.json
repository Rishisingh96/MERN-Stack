{
  "name": "thapa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -p . --watch",
    "tsc-file": "tsc",
    "start": "npm run build && node dist/index.js",
    "run": "npm run build && node dist"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
